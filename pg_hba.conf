# TYPE  DATABASE        USER            ADDRESS                 METHOD
# "local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256
# Allow all remote connections with password authentication
host    all             all             0.0.0.0/0               scram-sha-256
# Allow connections from internal docker network
host    all             all             172.18.0.0/16           scram-sha-256
# Allow connections from specific IPs (replace with your office/home IP)
host    userdb          postgres        YOUR_IP_ADDRESS/32      scram-sha-256 